package main

const (
	// Current username
	USER = "\\u"

	// Current hostname
	HOSTNAME = "\\H"

	// User and Hostname
	USER_HOSTNAME = USER + "@" + HOSTNAME

	// Current path
	PATH = "\\w"

	// Prompt : $ or users and # for root
	PROMPT = "\\$"
)
